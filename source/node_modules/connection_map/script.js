var deslocX = 100;
var deslocY = 100;

//Width and height
var w = 800 + deslocX;
var h = 600 + deslocY;

//Create SVG element
var svg = d3.select("body")
	.append("svg")
	.attr("width", w)
	.attr("height", h);

var geomap = new GeoMap({
	id: "myGeoMap",
	container: svg,
	x: deslocX,
	y: deslocY,
	width: w - deslocX,
	height: h - deslocY,
	dataset: shapes,
});
